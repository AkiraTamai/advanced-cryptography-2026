# Advanced Cryptography Program Output Themes

> 本ページは Advanced Cryptography Program 2026 の成果物テーマ案を、受講生向けに整理したものです。

### Intmax

#### 1. MPC Wallet の鍵復元をトラストレス化（Witness Encryption / Garbled Circuits）

- 背景：MPC ウォレットは鍵シェアを複数事業者が分散保管する方式が一般的だが、運営会社が事業停止・破綻すると鍵シェアにアクセスできなくなりユーザー資産が失われるリスクがある。現状の「リカバリーは運営に依存」という構造を暗号学的に解消したい。
- 方向性：Witness Encryption（特定の条件 = witness を満たした人だけが復号できる暗号）や Garbled Circuits を用いて、「条件成立時のみ鍵シェアが復元される」というオンチェーン or 分散ストレージ上の仕組みを設計・実装する。第三者の善意・存続に依存しない recovery を目指す。

#### 2. Channel-based Note Discovery × PIR による匿名性向上

- 背景：プライバシー決済プロトコルでは、自分宛の note（受取記録）を全 note 集合の中から見つける必要があり、ここで「誰がどの note を見たか」がリークしうる。[ia.cr/2026/474](http://ia.cr/2026/474) で提案された channel-based note discovery はこれを軽量化する手法だが、まだ匿名性に改善余地がある。
- 方向性：PIR（Private Information Retrieval：サーバに「どのデータを取得したか」を知られずにデータ取得する暗号技術）を channel-based note discovery と組み合わせ、「どの channel を購読しているか」自体も秘匿する設計を試みる。

#### 3. zERC20 Transfer Tree を活用した条件付き決済スマートコントラクト

- 背景：Intmax の zERC20 はトランザクションを Merkle Tree 構造（transfer tree）で集約することで、L1 のガスコストを抑えながらプライバシーを担保する。この構造をペイメント以外の用途、特に「条件が満たされたときだけ実行される決済」に応用できないか、というのが本テーマ。
- 方向性：transfer tree の包含証明（特定の transfer が tree に含まれている zk 証明）をトリガーとして発火する条件付き決済（エスクロー、ストリーミング支払い、条件付き送金など）の smart contract を設計・実装する。

#### 4. Clean による形式証明された Tornado Cash クローンの実装

- 背景：Tornado Cash 系のミキサーは smart contract の小さなバグが資産流出に直結する。Clean（[Verified-zkEVM/clean](https://github.com/Verified-zkEVM/clean/)）は Lean 4 ベースで zk 回路を形式検証できるフレームワークで、これを使えば「回路レベルで正しさが証明された」ミキサーが作れる。
- 方向性：Clean を用いて Tornado Cash の主要回路（commitment, nullifier, Merkle inclusion）を実装し、「二重使用が起こらないこと」「nullifier の一意性」などの安全性プロパティを Lean 4 で形式証明する。

---

### Nyx Foundation

#### 1. マルチシグ運用の正しさのゼロ知識証明

- 背景：DAO や財団のマルチシグは「本当に正しいメンバーが、正しい場所・状況で署名しているか」を外部から検証する手段がない。署名者が同じ部屋にいて強要されている、あるいは特定の管轄下にいる、といったリスクを暗号学的に弾きたい。
- 方向性：署名者の位置情報・タイムスタンプ・デバイス状態などの「運用条件」を入力に取り、「条件を満たしたうえで署名が行われた」ことを ZK で証明する仕組みを設計する。位置情報を漏らさずに「特定エリア内 / 外」だけを証明する range proof の応用が中心になる。

#### 2. 形式証明のゼロ知識証明（Lean 4 × zkVM）

- 背景：Lean 4 で書かれた Theorem + Proof が正しいことを確認するには、検証者自身が Lean 4 環境を構築して `lake build`（型検査）を通す必要がある。環境構築・依存解決・ビルドのコストが高いうえ、証明の中身（証明戦略や補題構成）を公開したくないケースでは、そもそも第三者による検証が成立しない。
- 方向性：Lean 4 の Theorem + Proof に対して「この Theorem のビルドがパスした」ことを、証明内容を明かさずに zkVM 上で証明する仕組みを設計・実装する。検証者は Lean 4 環境を持たず `lake build` を実行しなくても、zk 証明を検証するだけで「この定理は正しい」と納得できる。

#### 3. AI エージェント × Proof-of-Exploit による DeFi 自動停止

- 背景：DeFi のハッキングは発見から実行までが分単位で、人間のガバナンス投票では間に合わない。AI エージェントが脆弱性を見つけて即座にコントラクトを止められれば被害を最小化できるが、「誤検知で勝手に停止される」リスクをどう防ぐかが課題。
- 方向性：AI エージェントが脆弱性を発見した際、その exploit を実際に再現する Proof-of-Exploit（「この入力でこのコントラクトはこう壊れる」という検証可能な証拠）を生成。これをオンチェーンで検証し、有効な PoE が提出されたときのみコントラクトを pause する emergency stop 機構を実装する。

#### 4. SPECA を使った ZK/FHE/MPC 回路のバグ発見

- 背景：SPECA は仕様（specification）からチェックリストを生成して OSS のセキュリティ監査を行う Nyx の AI エージェント基盤。Sherlock contest で実績を出しているが、適用対象は主に Solidity の DeFi コントラクトに留まっている。ZK/FHE/MPC 回路のバグは「仕様レベルで何を満たすべきか」が曖昧で、人間の監査でも見逃しが多い領域。
- 方向性：SPECA の checklist 生成ロジックを ZK/FHE/MPC 回路向けに拡張し、実際の OSS 暗号回路（例：Tornado, Semaphore, zkEVM 関連）に適用してバグを発見する。出力として「ZK 回路向け監査チェックリスト」を一般化することを目指す。

---

### SMBC日興証券

#### 1. オフチェーン金融システムから DeFi コントラクトを呼び出す際のプライバシー保護

- 背景：証券会社が DeFi プロトコルを業務に組み込もうとすると、「どの顧客が、いつ、何の銘柄を、どれだけ」という注文情報がオンチェーン or API ログにそのまま残り、顧客情報保護・金商法・フロントランニングの観点で許容できない。
- 方向性：オフチェーンの注文システムと DeFi コントラクトの間に、ZK 証明や FHE、コミット-リビール、あるいは MPC ベースのリレーを置く設計を検討。「コントラクトの実行は正しいが、誰が何をしたかは事業者からも第三者からも見えない」状態を実現する API レイヤーを設計・実装する。

#### 2. TEE を用いた機密 LLM と ZK/MPC/FHE の組み合わせ

- 背景：証券会社のような規制産業は、顧客情報・取引情報を外部 LLM に投げられず AI 導入が大幅に遅れている。TEE（Intel SGX / TDX, AWS Nitro Enclaves など、ハードウェアで隔離された secure enclave 内でデータを処理する技術）を用いた confidential LLM は有力な解だが、(a) サイドチャネル攻撃や脆弱性（SGAxe, Downfall 等）でリークしうる、(b) ベンダー（Intel/AMD/NVIDIA）への信頼が必要、(c) attestation の検証コストが高い、といった課題が残る。
- 方向性：TEE 単体ではなく、ZK/MPC/FHE と組み合わせて「単一ベンダー信頼を分散させる」アーキテクチャを設計する。例：①TEE の attestation を ZK 証明で軽量化・オンチェーン検証可能にする、②前処理（埋め込み生成・トークナイズ）を FHE で行い、推論本体だけ TEE に渡す hybrid 構成、③複数 TEE ベンダー間で MPC を組み「どれか1つが破られても安全」な多重防御、④入出力を MPC で分割して TEE に渡し plaintext を再構成できない設計。FHE 単体推論より高速、TEE 単体より安全という効率と安全性のスイートスポットを狙う。

---

### キリフダ

#### AIエージェント向けテレマティクス保険のための ZK 証跡

- 背景：キリフダは AI エージェント向けのテレマティクス保険（エージェントの実際の行動データに基づいて保険料・補償を算定する保険）を構想中。保険を成立させるには「エージェントが実際にどう振る舞ったか」の証跡を改ざん不可能な形で残す必要があり、オンチェーン記録が自然な選択肢になる。一方で、行動ログをそのまま載せるとエージェントの戦略・顧客情報・取引相手などが全世界に公開されてしまい、商用利用に耐えない。
- 方向性：エージェントの行動ログ（API 呼び出し、判断、取引、外部サービスとのやり取りなど）に対して、ZK 証明で「特定の保険条件を満たす行動だったこと」「規約違反がなかったこと」「事故発生時に申告内容と実ログが一致すること」だけをオンチェーンで検証可能にする。生ログ自体はオフチェーンに保持し、必要な性質（しきい値以下のリスク行動、特定ポリシーへの準拠、claim 時の証拠整合性など）のみを ZK で開示する設計を目指す。自動車テレマティクス保険の「速度・急ブレーキ頻度の証明」を、AI エージェントの「行動ポリシー準拠の証明」に拡張するイメージ。

---

### HODL1

（テーマ追加予定）
